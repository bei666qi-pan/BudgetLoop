## 1. Brand asset and packaging

- [x] 1.1 Replace the temporary loop mark with a vector asset faithful to the supplied image and generate the macOS icon resource.
- [x] 1.2 Package the icon through `Info.plist` and `desktop/build.sh` without changing app signing behavior.

## 2. Launcher experience

- [x] 2.1 Rebuild the AppKit status window with branded hierarchy, readable stage feedback, and polished indeterminate activity.
- [x] 2.2 Add an accessible, reduced-motion-safe failure treatment while preserving redacted failure copy and exit behavior.

## 3. Verification

- [x] 3.1 Build and sign the app, verify icon metadata/resource, and inspect the vector mark plus an isolated native launcher smoke state.
- [x] 3.2 Run the required launcher UI design review and apply targeted corrections to the stage layout and reduced-motion-safe ambient treatment.
